[project]
name = "spacecraft-env"
version = "0.1.0"
description = "OpenEnv spacecraft simulation environment"
authors = [
    { name = "SriNidhi T" }
]
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "requests",
    "openai",
    "python-dotenv",
    "openenv-core>=0.2.0"
]

# ✅ REQUIRED
[project.scripts]
server = "server.app:main"
